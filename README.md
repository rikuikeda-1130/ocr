# OCR 固定リンク

商談で相手に渡す永久リンクです。

    https://rikuikeda-1130.github.io/ocr/

開くと、その時点で生きている OCR の公開URLへ自動で転送されます。
転送先（`url.json`）は Mac 側の監視スクリプトが自動で書き換えるため、
トンネルが張り直されても、このリンク自体は変わりません。

このリポジトリに入っているのは転送用のページだけです。
アプリのソース・パスワード・OCRデータは一切含まれていません。
